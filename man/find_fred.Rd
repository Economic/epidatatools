% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fred_search.R
\name{find_fred}
\alias{find_fred}
\title{Find FRED Series by Search String}
\usage{
find_fred(
  search_string,
  max_results = 20,
  metadata = FALSE,
  seasonality = NULL,
  fred_api_key = Sys.getenv("FRED_API_KEY")
)
}
\arguments{
\item{search_string}{Character string to search for in series titles}

\item{max_results}{Maximum number of results to return (default: 20)}

\item{metadata}{Logical flag to retrieve additional metadata (default: FALSE). When FALSE, only series_id and series_title are returned.}

\item{seasonality}{Optional filter for seasonal adjustment: NULL (no filter, default), "NSA" (Not Seasonally Adjusted), or "SA" (Seasonally Adjusted)}

\item{fred_api_key}{FRED API key (defaults to FRED_API_KEY environment variable)}
}
\value{
A tibble with columns series_id, series_title, and optionally metadata (a list column containing additional metadata when metadata = TRUE)
}
\description{
Searches the \href{https://fred.stlouisfed.org/}{FRED database}
for series matching a search string.
}
\details{
This function is a wrapper around \href{https://sboysel.github.io/fredr/}{\code{fredr::fredr_series_search_text()}}
and requires you to have an \href{https://fred.stlouisfed.org/docs/api/api_key.html}{FRED API key}.
}
\examples{
\dontshow{if (nzchar(Sys.getenv("FRED_API_KEY"))) withAutoprint(\{ # examplesIf}
find_fred("unemployment rate")

find_fred("GDP", max_results = 10)

find_fred("inflation", metadata = TRUE)

find_fred("employment population ratio", seasonality = "SA")
\dontshow{\}) # examplesIf}
}
